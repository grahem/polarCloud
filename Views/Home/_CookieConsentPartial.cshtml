    <div id="cookieConsent" class="alert alert-info alert-dismissible fade show cookies" role="alert">
        We use cookies to personalize content and to analyse our traffic. We do not share your information.
        <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>.
        <button type="button" id="btn-accept" class="accept-policy close">
            <span aria-hidden="true">Accept</span>
        </button>
    </div>
    <script>

        function getCookie(cname) {
            var name = cname + "=";
            var decodedCookie = decodeURIComponent(document.cookie);
            var ca = decodedCookie.split(';');
            for(var i = 0; i <ca.length; i++) {
                var c = ca[i];
                while (c.charAt(0) == ' ') {
                c = c.substring(1);
                }
                if (c.indexOf(name) == 0) {
                return c.substring(name.length, c.length);
                }
            }
            return "";
        };

        document.getElementById('btn-accept').addEventListener("click", function (event) {
            document.cookie = "consent=true";
            document.getElementById('cookieConsent').style.display ="none";
            
        });

        
        if (getCookie('consent') === "true") {
            
        document.getElementById('cookieConsent').style.display ="none";            
        };
    </script>
